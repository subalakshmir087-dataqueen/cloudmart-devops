replicaCount: 1

image:
  repository: us-central1-docker.pkg.dev/gcp-devops-project-498805/cloudmart-repo/orders-api
  tag: "v1"
  pullPolicy: IfNotPresent

service:
  type: ClusterIP
  port: 8080

resources:
  requests:
    cpu: "100m"
    memory: "128Mi"
  limits:
    cpu: "200m"
    memory: "256Mi"
